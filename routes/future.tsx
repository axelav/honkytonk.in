import { PageProps } from '$fresh/server.ts'
import { PageHeading } from '@/components/typography.tsx'

const FuturePage = (props: PageProps) => <PageHeading>Future</PageHeading>

export default FuturePage
