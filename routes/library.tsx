import { PageProps } from '$fresh/server.ts'
import { PageHeading } from '@/components/typography.tsx'

const LibraryPage = (props: PageProps) => <PageHeading>Library</PageHeading>

export default LibraryPage
